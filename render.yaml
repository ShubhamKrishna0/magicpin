services:
  - type: web
    name: vera-merchant-bot
    runtime: docker
    plan: free
    envVars:
      - key: LLM_PROVIDER
        value: openai
      - key: LLM_API_KEY
        sync: false  # you'll set this in Render dashboard
      - key: LLM_MODEL
        value: gpt-4o-mini
